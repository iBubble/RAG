## 🚀 Release [2026-04-04 01:07:51] 自动推送更新

### 新增特性
- 发布 KimiDocx 技能模块（SKILL.md），提供 .docx 文档生成与编辑的完整操作指南。
- 确立“保留格式优先”与“模板即表单”的核心原则，明确区分纯文本读取与 XML 结构解析的适用场景。
- 规范 C# SDK 用于文档创建、Python (lxml) 用于文档编辑的技术栈分工，明确禁止使用 python-docx/docx-js 作为兜底方案。
- 构建统一的脚本入口与执行环境，支持 `init`、`build`、`validate` 等自动化命令，集成 Morandi 与水墨风格背景生成及专业图表绘制工具。

### 优化调整
- 优化环境检测逻辑，自动识别并适配 macOS 下 `dotnet` 与 `python3` 的多种安装路径。
- 增强构建脚本的容错能力，针对 `timeout` 命令缺失场景自动降级处理。
- 标准化工作目录与输出路径约定，支持通过环境变量自定义输出位置。
- 集成 OpenXML 验证器与 Python 验证链，实现构建后的自动化格式合规性检查。

---

## 🚀 Release [2026-03-31 16:01:12] 自动推送更新

### 新增特性
- 新增 `sync.py` 自动化脚本，集成本地 Qwen 大模型，实现根据 Git 变更自动生成专业 Release Notes 并同步更新 README 与发布记录。

### 缺陷修复
- 修复文档上传后未触发后台文本抽取与 ChromaDB 向量索引的严重 BUG，通过引入 BackgroundTasks 重构异步处理流程，并上线回溯补偿脚本以全量补录历史遗漏数据。

### 优化调整
- 重构 `.gitignore` 配置，彻底排除 `/docs` 目录并清理历史提交中的超大文件记录，优化仓库体积与性能。

---

