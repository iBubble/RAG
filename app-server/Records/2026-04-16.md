# 工作状态存档 (ShengyaoRAG - Micro-Checkpoint)

📅 2026-04-16T01:37:00+00:00
✅ Done:
- 完成 5 级 RAG 管线基准评估：Reranker 冷启动 320s（含模型下载），精排耗时 ~5.9s/query，在 3/5 个查询中纠正了 RRF 排序偏差。
- 上下文膨胀验证通过：10 chunks 成功扩展为 26 chunks，全管线检索耗时 6.13s。
- 评估脚本归档于 debug/evaluate_rag.py。
⏳ To-Do:
- 将 bge-reranker-v2-m3 模型预下载固化到 Docker 镜像内，消除首次 320s 冷启动。
- Reranker 精排耗时 ~6s 偏高（CPU 推理），评估 ONNX 量化或更轻量级 Reranker 模型。

---

# 工作状态存档 (ShengyaoRAG - Micro-Checkpoint)

📅 2026-04-16T02:00:00+00:00
✅ Done:
- P0 完成：Dockerfile 新增模型预下载层，BGE-M3 + Reranker 将在镜像构建时固化到缓存，消除 320s 冷启动。
- 5 级 RAG 管线基准评估已完成（Reranker 精排 ~5.9s/query，3/5 查询排序被纠正）。
- 上下文膨胀验证通过（10→26 chunks），全管线 6.13s。
⏳ To-Do:
- 宿主机执行 docker-compose up -d --build 重建镜像使预下载生效。
- Reranker CPU 推理 ~6s 偏高，评估 ONNX 量化或更轻量 bge-reranker-base。

---

# 工作状态存档 (ShengyaoRAG - Micro-Checkpoint)

📅 2026-04-16T10:11:00+00:00
✅ Done:
- 解决前端 SSE 代理阻塞：为 Vite proxy 新增 configure 回调，禁用 text/event-stream 的缓冲，实现实时流式响应到页面。
- 诊断后端 PM2 重启：由系统或 OOM 重加载引起，当前服务自动重组稳定在内存 (~1.4G/4G)，后台功能一切正常。
- 明确文本未生成根因：宿主机的 Ollama `qwen3.5:35b-q4` API 推理发生中断，吐出 `<think>` 标签后立刻发送了 done=true 强行截止。
⏳ To-Do:
- 排查/升级宿主机的 Ollama 模型端配置或更换微调模型版本，修复长文生成时提前被截断或空截流 Bug。
- 根据真实运行情况考虑是否通过切换更轻量的模型或降配重发。

---

# 工作状态存档 (ShengyaoRAG - Micro-Checkpoint)

 Done:
- 排查确认 qwen3.5:35b-q4 提前截流 Bug 的根因为模 Modelfile 缺少停止符 `<|im_start|>` & `<|im_end|>` 配置。
- 排查后端 `llm_engine.py` 参数，发现 `repeat_penalty: 1.1` 会过度抑制 DeepSeek 蒸馏模型的 `<think>` 推演机制，导 EOS。
已出具修正方案（implementation_plan.md）并在挂起 - Master 审定。
   To-Do:
- Master 决策后，在宿主机运用新 Modelfile 执行 `ollama create` 重新打包模型。
- 修改 `llm_engine.py` 去除或调低 `repeat_penalty` 避免输出截断。
