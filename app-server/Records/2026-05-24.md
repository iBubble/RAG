📅 本次时间戳：2026-05-24T22:20:00+08:00
✅ Done (核心突破)：
- 彻底解决预计算任务卡在“项目背景”章节的死锁故障。通过将系统大模型调用的 `num_ctx` 全局统一并限制为固定常数 `16384`，消除由于 reranker、GraphRAG、slot 抽取等在不同 context 切换时引发的 Ollama 卸载并重新加载模型引起的巨额耗时和超时死锁。
- 重启 PM2 容器服务，验证 `test_rerank.py` 和 `test_stream_ollama.py` 均在一秒级内成功响应。慢队列预计算任务目前流畅运行，不再在第一章卡死，正顺序向下计算后续 200 多个章节。
- 清理了系统中 test_*.py 临时测试脚本，保持代码库整洁。

⏳ To-Do (待办事项)：
- [ ] 观察预计算在后台完全跑完 207 章节。
- [ ] 引导用户上传新项目的具体地名（毗卢镇等）参考资料，验证变量智能替换的完全替换正确率。

---

📅 本次时间戳：2026-05-24T21:30:00+08:00
✅ Done (核心突破)：
- 定位并修复了 Celery Worker 启动预计算任务时，由于 `ecosystem.config.js` 缺失 `JWT_SECRET` 环境变量，导致在导入 `core/auth_deps.py` 时抛出 `RuntimeError` 崩溃而导致任务卡在“待触发”状态的问题。
- 重构了 `ecosystem.config.js`，将 `JWT_SECRET` 动态解析逻辑提取为常量，并完整注入到 `shengyao-celery-fast` 与 `shengyao-celery-slow` 的 env 配置中。
- 重置并重新启动了 PM2 容器服务（`pm2 delete all && pm2 start`），验证四服务全部常驻 online 且无报错。

⏳ To-Do (待办事项)：
- [ ] 请用户重新在前端点击触发预计算，观察后台进度条和任务状态变化。

---

📅 本次时间戳：2026-05-24T21:20:00+08:00
✅ Done (核心突破)：
- 智能预计算重构：成功拆分“一键生成全文”、“范文智能替换”、“范文精确复刻”三模式预计算。
- 全链路打通：后端通过 Celery 异步流式生成并以 Markdown 缓存到磁盘，前端检测到缓存后通过 200ms 逐章节滚动动画填充渲染，极大节约生成时间。
- 体验优化与资源隔离：在预计算章节间引入 1.0s 延迟避开 GPU 实时竞争，并在入库资料及范文变更时自动清除关联缓存防止使用过期数据。

⏳ To-Do (待办事项)：
- [ ] 引导用户在前端挂载范文后测试“范文智能替换预计算”及“范文精确复刻预计算”
- [ ] 在管理后台“学习进度看板”实时监控三种预计算模式 of Celery 后台进度

---

📅 本次时间戳：2026-05-24T22:55:20+08:00
✅ Done (核心突破，限3条极简 bullet)：
- 彻底解决预计算进度不动的死锁故障。通过在 `core/llm_engine.py` 的模型预热 `warmup_model` 和心跳守护 `_heartbeat_loop` 的 Ollama HTTP 请求中显式传入 `options: {"num_ctx": 16384}`，限制大模型的 context 显存分配，避免其默认分配 256K 巨型 context 撑爆 Mac Studio 显存与内存引起严重的 Swap 推理假死。
- 清理了系统中多余的预计算孤儿进程（PID 879610），并重启了 PM2 中的 `shengyao-backend` 后端和 `shengyao-celery-slow` worker 进程，安全释放了被阻塞的队列连接。
- 成功重新调度预计算任务。项目第一章（项目概要）与第二章（项目背景）均已在 16K 显存优化下顺畅通过 RAG 精排检索与 LLM 生成并写入物理缓存 JSON 文件，后台队列现已安全有序推进。

⏳ To-Do (待办事项，限3条极简 bullet)：
- [ ] 观察预计算在后台完全跑完 207 章节。
- [ ] 引导用户观察前端进度条更新情况，确认已正常上涨运行。
