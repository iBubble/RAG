📅 2026-04-12
✅ Done: 
- 修正了一键清除后端不同步问题，在 DocumentStudio 增加强制覆盖接口。
- 修复了 AI Persona 生成的寻址报错及缺失指令（要求强中文格式与专业工程约束）。
- 解决了大纲模版双端同步失效问题，彻底剔除了 Zustand 持久化中的 templateSections 脏数据污染与 auto-save 逆向覆盖服务器问题，加入 no-cache 请求头。
⏳ To-Do: 
- 验证多端同步问题是否彻底解决，观察是否存在其他持久化缓存覆盖行为。
- 根据用户意见微调导出的样式排版。
- 优化了 AI 智能推荐大纲时的角色规范（添加了“# 知识调用规范”），强迫大模型在项目背景、行业标准、国家政策等方面调用自身庞大的预训练知识库，深度发散与补充。
- 修复了 AI Persona 生成时 DeepSeek-R1 模型 `<think>` 标签未闭合导致的输出异常和字数截断问题（放宽至 2048 Token 并优化正则匹配）。
- 扩大了 Ollama 的 num_predict 额度限制上限（2048 -> 8192），彻底解决深思熟虑模型思考链条过长带来的结果为空问题。
- 修复了 AI 助手面板中智能推荐大纲时未采纳下拉框所选模型的问题，已将所选模型绑定至推荐接口请求内。
- 彻底修复 `think_filter.py` 内部可能由于暂存 `buffer` 而导致 SSE 事件流生成时静默卡死无输出（即长时间一条结果都没有吐出导致前端一直转圈）的问题，补全了生成器结束时的 `buffer` 推送逻辑。
- 彻底禁用 Mermaid 流程图生成：从 DUAL_TRACK_PROMPT 中移除了鼓励模型生成 ```mermaid 的指令，改为明确禁止；同时在 SSE 流式输出管道中新增了 `_strip_mermaid_blocks` 安全过滤器，即使模型仍然自说自话生成了流程图代码块，也会在到达前端/Word 导出之前被完整剥离，确保 Word 文档中不再出现无法渲染的代码乱文。
- 修复 Word 导出中 Mermaid 代码块显示为乱码的问题：在 export.py 的两条导出路径（Python 引擎 `_render_section_content` 和 C# 引擎）中，均新增了 `re.sub(r'```mermaid\b.*?```', ...)` 正则剥离逻辑，确保不论内容是新生成的还是旧数据中遗留的，导出 Word 时流程图代码块都会被完整移除。同时将原先的"降级输出代码块纯文本"逻辑替换为静默跳过。
- 解决 Ollama GPU 资源被长文档段落批量生成长时间阻塞的问题：配置 `OLLAMA_NUM_PARALLEL=2` 开启双路并行推理，并在后端新增 `LLMPriorityScheduler` 基于最小堆的优先级调度器。确保聊天问答、角色推荐等短耗时指令实现抢占调度，达成极速响应。
- 开启 Ollama Flash Attention + KV Cache q8_0 量化。Prompt 处理速度从 24.79 提升至 91~702 tok/s（取决于长度），生成速度稳定 45 tok/s 无退化。显著降低首 Token 延迟（TTFT）。
- 实现语义感知切片器 `_semantic_chunk()`（替代暴力512字符切割），按段落边界+句子边界切割，Markdown 表格作为不可分割原子保护。新增 PDF 表格结构化提取 `_extract_tables_pdf()`，利用 PyMuPDF find_tables() API 从 PDF 中检测并提取完整表格。
