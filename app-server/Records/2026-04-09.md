⏳ To-Do:
- [x] 同步前端代码，确保 `chat_mode` 高级参数在 UI 端实时透传。（经审查：前端已实现四模式 UI 切换 + fetch 透传，闭环完成）

---

📅 2026-04-09 09:10 (Web 导入资料预览修复)

✅ Done:
- 【根因定位】web/text 来源无扩展名 + 无磁盘文件，FilePreviewer 的文件格式判断全部落空，命中"格式暂不支持"兜底。
- 【后端新增】`vector_store.get_all_chunks()` 从 Qdrant 按 file_id 反查全文；`web_ingest.py` 新增 `/preview/{source_id}` 接口。
- 【前端修复】`FilePreviewer` 检测 `source_type === web|text` 走专用预览路径，渲染带来源标签的文本预览视图。

---

📅 2026-04-09 10:00 (Chat fast 模式空回复 + 范文复刻结构错误修复)

✅ Done:
- 【Chat 空回复根因】fast 模式下 qwen3.5 默认先生成 `<think>` 推理链，面对 327 条 SHP 村级记录时 think 耗尽全部 8192 token 预算，导致正式回答 0 字。修复：fast 模式注入 `/no_think` + `num_predict`/`num_ctx` 提升至 16384。
- 【范文复刻根因】`_is_structural_heading()` 硬编码 `return False`（形同虚设），导致范文中 ~30 个无正文的纯结构标题（L1/L2 大标题）也被大模型凭空生成正文，破坏范文结构。修复：从范文 JSON 读取 content，空则返回 skip 信号跳过生成。
- 【已部署验证】94 个章节中 EMPTY/HAS 判定全部正确，后端 PM2 重启无报错。
