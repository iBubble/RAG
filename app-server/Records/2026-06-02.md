📅 2026-06-02 19:11 (CST)

✅ Done:
- 在 `data_analyzer.py` 的 `analyze_data` 中部署双重防护机制：1) 强行将 SQL 翻译与自愈纠错引擎锁定在 35B 大模型，与聊天会话主模型解耦；2) 引入空值与全 NaN 执行结果异常捕获逻辑，自动将其触发为自愈重试。
- 优化 `_SQL_SYSTEM_PROMPT` 中的 Rule 8（列名精确对应）与 Rule 13（精简防误匹配指示），移去复杂的补零说明，代之以直观的 few-shot 案例，解决了大模型在混合统计下将编号写错或算错的 Bug。
- 经过端到端运行验证，计算 155#、27#、36# 地块合计面积与宽度过滤的问题无论在 8B 还是 35B 会话下均自动生成了完全精准、无零前缀误伤且主指标不漏算的 SQL，正确输出了 0.9161 公顷。

⏳ To-Do:
- 针对前台不同地块编号与多条件聚合的并发复杂查询场景进行压力校对。

---

📅 2026-06-02 18:55 (CST)

✅ Done:
- 在 `_SQL_SYSTEM_PROMPT` 中新增避免模糊匹配误伤规则（Rule 13），指导大模型在过滤编号、标识符等具子串重叠关系的字段时禁止使用宽泛 of `%LIKE%`，优先采用 `IN` 等值匹配或补零对齐。
- 经过端到端运行验证，计算 155#、27#、36# 地块合计面积问题中，大模型生成的 SQL 自动改用 `整治地块编号 IN ('开发复垦155#', '开发复垦027#', '开发复垦036#')` 精确过滤，彻底杜绝了 127# (含 27#) 和 136# (含 36#) 地块的混入，计算值精准回归至 0.9161 公顷。

⏳ To-Do:
- 针对前台复杂联合筛选地块编号等多样提问模式，强化模糊与精确转换边界测试。

---

📅 2026-06-02 18:34 (CST)

✅ Done:
- 在 `_SQL_SYSTEM_PROMPT` 中新增“仅有/只有”逻辑转换规则（Rule 12），明确禁止大模型在遇到排他性问题时生成单一等值 `WHERE` 过滤，强制其使用 `EXCEPT`（差集排除）或 `HAVING` 聚合判定。
- 经过端到端运行验证，“筛选仅有土埂的地块”问题自动生成了标准的 `EXCEPT` SQL 查询，完美将混合材质地块（如既有土埂又有石埂的开发复垦 004#）从结果中剔除，结果行数由原先错误的 146 行降为精确的 8 行。

⏳ To-Do:
- 针对更多排他性业务指标进行自适应生成测试，确保 8B 小模型在各种差集 SQL 关联下的稳定性。

📅 2026-06-02 19:20 (CST)

✅ Done:
- 优化 `core/data_analyzer.py` 中合并单元格列的自动检测算法，通过在分组键（整治地块编号）下检查组内的非空唯一值个数变化比例，有效隔离了田坎宽度、高度、面积等类别/多值属性字段被误判为合并列。
- 修复了 SQL 数据分析中大模型由于去重提示而对“宽度”与“田坎面积”使用 `FIRST()` 进行 GROUP BY 聚合，导致计算大于等于 1 米的田坎面积之和为 0 的 Bug。
- 重新验证数据查询，在宿主机与容器环境测试正常。

⏳ To-Do:
- 对多地块的其它混合多属性聚合统计进行全面边界用例校对。
