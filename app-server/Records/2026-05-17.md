# 工作状态备忘录

📅 2026-05-17 08:22

## ✅ Done
- **Vision OCR 重入库完成**：276/276 文件全部成功，274 个 Vision LLM 解析 + 2 个 OCR 降级
- **管理看板新增重入库进度横幅**：后端 `/api/admin/reingest-progress` + 前端实时进度条
- **正则 Bug 修复**：总结行 `❌ 失败: 0` 被误计为 1 次失败，用 `^\s+` 前缀限定修复
- Vision Prompt 优化（分析型→OCR型）+ DPI 200→300 + 分辨率 1280→2048
- Vision 模型名修复 `qwen2.5-vl:7b` → `qwen2.5vl:7b`
- num_ctx 16384→32768 解决表格上下文截断

## ⏳ To-Do
- 清理 generate.py 中的临时调试 print 语句
- 排查 2 个仍降级到 OCR 的文件（Vision 可能超时）
- 监控 Qdrant 向量切片总数变化，确认数据一致性
# 工作状态备忘录

📅 2026-05-17 08:32

## ✅ Done
- **Shapefile 流式统计引擎**：`_extract_shp_dbf()` 重构为双模式——≤5万条 DataFrame 全量 / >5万条流式迭代（Counter+累加器），内存恒定 ~100MB，支持数十万图斑
- **Vision OCR 重入库完成**：276/276 全成功，274 Vision + 2 OCR 降级
- **管理看板重入库进度横幅**：`/api/admin/reingest-progress` + 前端实时卡片
- **正则 Bug 修复**：总结行误计为失败

## ⏳ To-Do
- 清理 generate.py 临时调试 print 语句
- 排查 2 个 OCR 降级文件（Vision 超时原因）
- 用真实大规模 SHP 测试流式统计路径
# 工作状态备忘录

📅 2026-05-17 08:54

## ✅ Done
- **P2 Chunk 去重**：`ingest_text()` 切片后 set 去重 + doc_summary 与首 chunk 相同时跳过
- **P1 OCR 噪声清理**：清除 pdf.py/image.py/vision_extractor.py 共 4 处元数据头部模板
- **SHP 流式统计**：>5万条图斑走 Counter+累加器流式路径，内存恒定 ~100MB
- **Vision OCR 重入库**：276/276 全成功

## ⏳ To-Do
- 清理 generate.py 临时调试 print 语句
- 用真实大规模 SHP 测试流式统计路径
- 考虑对已入库的 276 文件重入库以清理历史噪声头部
# 工作状态备忘录

📅 2026-05-17 09:04

## ✅ Done
- **P3 可信度标记**：`_compute_chunk_confidence()` 4维评分（CJK密度/文本长度/噪声检测/术语命中），存入 Qdrant payload `confidence` 字段
- **P2 Chunk 去重**：切片后 set 去重 + doc_summary 与首 chunk 相同时跳过
- **P1 OCR 噪声清理**：清除 pdf/image/vision 4处元数据头部模板
- **SHP 流式统计**：>5万条走 Counter+累加器，内存恒定 ~100MB
- **Vision OCR 重入库**：276/276 全成功

## ⏳ To-Do
- 清理 generate.py 临时调试 print 语句
- 对已入库 276 文件重入库以应用 P1/P2/P3 改进
- 在 generate.py 中利用 confidence 对低可信度来源添加提示


# 工作状态备忘录

📅 2026-05-17 09:12

## ✅ Done
- **清理与防幻觉注入**：移除 `generate.py` 中底层 token 级的冗余 debug print，且在拼接 RAG context 时，针对 `confidence < 0.4` 的段落动态追加低可信度预警，降低 LLM 幻觉率
- **P3 可信度标记**：`_compute_chunk_confidence()` 4维评分存入 Qdrant
- **P2 Chunk 去重**：切片后去重 + 移除冗余 doc_summary
- **P1 OCR 噪声清理**：清除 4 处硬编码模板
- **Vision OCR 重入库**：276/276 全成功

## ⏳ To-Do
- 对已入库的 276 个 OCR 文件执行重入库，使 P1/P2/P3 规则落地到 Qdrant 向量数据中
- 选定真实的大规模 SHP 数据，验证流式分析引擎性能


# 工作状态备忘录

📅 2026-05-17 09:55

## ✅ Done
- **P2 PPTX 表格注册**：新增 `_extract_tables_pptx()`，PPT 中的表格现在也会提取并注册到表格注册表，供报告生成时精确注入
- **P3 看板口径统一**：在 `status_tracker.py` 中定义 `EXCLUDED_STATUSES` + `EXCLUDED_REASON_MAP` 共享常量，`admin.py`（管理员学习进度）和 `projects.py`（项目知识库进度）统一引用，排除 empty_text/unsupported_format/too_large/failed 文件后的 total 完全一致
- **generate.py 防幻觉**：低可信度 chunk (confidence<0.4) 自动追加 OCR 警告提示
- **P1-P3 OCR/去重/可信度**：已完成并部署

## ⏳ To-Do
- 对已入库的 276 个 OCR 文件执行重入库，使 P1/P2/P3 规则落地到 Qdrant 向量数据中
- 容器内安装 libreoffice 解决 .doc 格式在 Docker 中不可用的问题
