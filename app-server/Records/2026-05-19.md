📅 2026-05-19 02:44

## ✅ Done
- Table Registry 代码审查确认注册链路完整（RAID 上已有 37 个 JSON），在线验证待 RAID 恢复
- `graph_rag.py` 新增 `delete_by_file_id()` 增量删除 + 实体缓存自动失效
- `worker.py` 图谱抽取前先删旧三元组，实现幂等增量重建
- `worker.py` 社区摘要触发加 Redis SET NX 锁（10 分钟窗口防重复调度）
- 社区摘要独立队列：`summary_queue` 与 `slow_queue` 解耦，图谱提取不再被插队

## ⏳ To-Do
- `pm2 delete shengyao-celery-slow && pm2 start ecosystem.config.js --only shengyao-celery-slow` 使队列改动生效
- RAID I/O 恢复后运行 `verify_table_registry.py` 验证表格向量完整性
- 评估 slow_queue concurrency 提升至 2 的可行性
