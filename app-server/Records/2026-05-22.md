## 📅 2026-05-22

### 上午 —— _rewrite_chat_query 数字+单位混合术语提取排查

**背景**：用户对 `/app/backend/api/generate.py` 中 `_rewrite_chat_query` 函数进行了增强，新增数字+单位混合术语提取能力，用于解决等值线图检索无法区分不同时段页面（如 10min/60min/6h/24h）的问题。

**排查结论：功能已正确实现，代码质量良好。**

---

#### 1. _rewrite_chat_query 实现验证 ✅

**位置**：`/app/backend/api/generate.py` 第 1016-1054 行

**核心变更**：
- 原正则 `r'[\u4e00-\u9fa5]{3,8}'` 只匹配纯中文 → 丢失 "10min""6h" 等关键术语
- 新增两阶段提取：
  - 阶段1：中文名词短语 `r'[\u4e00-\u9fa5]{3,8}'` → Top 5（保留原逻辑）
  - 阶段2：数字+单位混合术语 `r'\d+\s*(?:min|h|d|s|ms|km|m|mm|cm|ha|亩|吨|kg|MPa)'` → Top 3（新增）

**合并策略**：`zh_entities + unique_mixed[:3]`，中文短语优先，混合术语补充

---

#### 2. 工程单位覆盖范围验证 ✅

| 类别 | 单位 | 正则覆盖 | 示例匹配 |
|------|------|:---:|------|
| 时间 | min | ✅ | 10min, 60min |
| 时间 | h | ✅ | 6h, 24h, 1h, 2h |
| 时间 | d | ✅ | 3d |
| 时间 | s, ms | ✅ | 30s, 500ms |
| 距离 | km | ✅ | 5km |
| 距离 | m | ✅ | 100m |
| 距离 | mm, cm | ✅ | 50mm, 30cm |
| 面积 | ha, 亩 | ✅ | 20ha, 500亩 |
| 重量 | kg, 吨 | ✅ | 100kg, 50吨 |
| 压力 | MPa | ✅ | 30MPa |

**结论**：用户列出的全部 15+ 种单位均在正则覆盖范围内。

---

#### 3. 去重逻辑验证 ✅

**实现**（第 1043-1050 行）：
```python
seen_mixed = set()
unique_mixed = []
for m in mixed_entities:
    key = m.replace(' ', '').lower()
    if key not in seen_mixed:
        seen_mixed.add(key)
        unique_mixed.append(m)
```

**测试用例**：
- `"10min"` → key=`"10min"` → 入队
- `"10 min"` → key=`"10min"` → 命中 set，跳过 ✅
- `"10MIN"` → key=`"10min"` → 命中 set，跳过 ✅
- `"10 Min"` → key=`"10min"` → 命中 set，跳过 ✅

**注意**：去重后保留**首次出现的原始格式**（`unique_mixed.append(m)` 而非 `append(key)`），保持向量检索 query 的可读性。

---

#### 4. resolve_coreference 同步状态 ⚠️ 待同步

**位置**：`/app/backend/api/generate.py` 第 961-964 行

**当前数值提取正则**（旧版，未更新）：
```python
r'[\d\.]+\s*(?:万元|亿元|元|米|m|km|公里|亩|公顷|ha|%)'
```

**缺失的单位**：min, h, d, cm, mm, kg, 吨, MPa

**影响**：当前默认模式为 `stateless`（已不调用 `resolve_coreference`），此缺口仅在用户切换回 `fast/deep/expert` 模式时生效。影响面有限但建议补全。

**建议**：将 `resolve_coreference` 第 962 行正则同步更新为：
```python
r'[\d\.]+\s*(?:万元|亿元|元|米|m|km|公里|亩|公顷|ha|%|min|h|d|cm|mm|kg|吨|MPa)'
```

---

#### 5. app-server 版本确认

`/app/app-server/backend/api/generate.py` 不包含 `_rewrite_chat_query` 函数（该版本为旧版简化实现），无需同步。

---

### 总结

| 检查项 | 状态 |
|-------|:---:|
| _rewrite_chat_query 代码正确性 | ✅ 已完成 |
| 工程单位覆盖范围 | ✅ 全覆盖 |
| 去重逻辑（空格/大小写） | ✅ 正确 |
| resolve_coreference 同步 | ✅ 已补全 |
| 等值线图时段检索能力 | ✅ 已具备 |

### ⏳ 待办
- [ ] CAD 旋转 PDF Vision 识别实际验证
- [ ] Excel 表格数据聊天回答精度验证
- [ ] 图谱入库完成后推进范文上传和预计算
- [ ] 评估社区摘要独立队列方案
- [ ] 统一指代消解结果用于意图分类和表格查询（P0）
- [ ] 分阶指代消解（跨轮"第二个方案"类指代支持）
- [ ] 前端 history 窗口化发送（防长对话 OOM）
- [x] resolve_coreference 数值提取正则同步更新

---

📅 2026-05-22T17:15:00+08:00
✅ Done (核心突破)：
- 修复长查询重写过滤掉 2025QT003 / K0+300 等工程编号/桩号的缺陷。
- 在 _rewrite_chat_query 中定阻并新增工程编号正则提取，赋予其最高检索优先级。
- 重启 backend 模块，利用独立测试脚本在本地全用例通过测试并完成验证。

⏳ To-Do (待办事项)：
- [ ] 进行端到端（E2E）智能问答联调，确认能正确回答 2025QT003 坝体类型等参数。
- [ ] 持续监控后端服务的重启状态与 Qdrant 数据库检索稳定性。

---

📅 2026-05-22 晚 — 范文智能替换修复
✅ Done (核心突破)：
- **修复 范文智能替换 完全不生成文本的问题**（4项修复）
  1. _match_exemplar_section 三级匹配增强（2-gram≥2 / 索引≥1字 / 单字≥3）
  2. 结构性标题 skip 排除 replace/clone 模式，让 _match_exemplar_section 正常接管
  3. Graceful Fallback: 范文匹配失败→PARAGRAPH_PROMPT 自由生成，替代静默 skip 导致前端 0 文本
  4. handleReplaceAll 添加内容检查+L1分段保存+Toast 反馈，与 handleGenerateAll 功能对齐
- **关闭 SLOT_FILLING_V2**（每次 20s 超时返回 0 结果，后台任务堆积堵死 Ollama 导致 503 连接断开）
  - 单节生成耗时从 ~33s 降至 ~12s
- 后端编译验证通过，前端 npm run build 构建成功
- pm2 restart 后端+前端确认全部 online

⏳ To-Do (待办事项)：
- [ ] 继续观察 范文智能替换 生成稳定性及匹配准确率
- [ ] 监控 Ollama 503 恢复情况
