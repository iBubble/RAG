
### 📅 2026-04-11 07:30 (图件重复插入根治 + 表格样式全面美化)
#### ✅ Done
- 【图件重复根治】三级联动修复：(1) exemplar.py 范文解构时图片标记从正文剥离存入独立 images 字段；(2) generate.py 新增 _strip_image_markers() 清理残留 + _get_section_image_hint() 精确按章节注入图件指令；(3) llm_engine.py 三套 Prompt 模板图件指令改为条件式——无图件指令时严禁生成。
- 【表格样式美化】在 index.css 注入 Tiptap 表格全局 CSS：深色渐变表头、斑马纹、圆角、细边框、行悬停高亮、紧凑行距、resize 手柄美化。

#### ⏳ To-Do
- 用户需重新上传范文（或重新解构已有范文）使新的 images 字段生效。
- 持续监控生成效果确认图件不再重复。

---

### 📅 2026-04-11 07:35 (图件修复+表格美化已上线 / 大型数据表格截断问题留存)
#### ✅ Done
- 【图件重复根治】三级联动修复完成：exemplar.py 图片标记剥离 → generate.py 残留清理+精确注入 → llm_engine.py Prompt 条件式指令。已部署上线。
- 【表格样式美化】Tiptap 表格全局 CSS 注入完成：深色渐变表头、斑马纹、圆角、细边框、行悬停高亮。已部署上线。

#### ⏳ To-Do
- 🔴 **【留存待优化】大型数据表格完整性问题**：一键生成全文时，含大量行的统计表格（如"表20 蓬溪县各基本评价单元主要农产品年产量统计表"）只列出部分数据。根因为三重截断级联：(1) chunk_size=512 字符切片过小，一张大表被打散为十几个碎片；(2) RAG 检索 n_results=8 只召回部分切片；(3) LLM 输出 num_predict=8192 token 不足以完整输出大表。未来优化方向：入库时标记表格型 chunk 实现按表名全量召回 + 对含表格章节动态扩大检索量和输出预算 + Prompt 追加完整列举指令。

---

### 📅 2026-04-11 10:05 (范文智能替换功能完全失效的根因修复)
#### ✅ Done
- 【范文智能替换彻底修复】定位到"范文智能替换"按钮点击后无任何反应的根因：`handleReplaceAll` 照搬了 `handleGenerateAll` 的跳过逻辑（`cleanText.length > 5` 即跳过），导致所有已经有过"一键生成"内容的章节全部被跳过。修复方案：移除替换模式的跳过条件，因为替换的核心目的就是覆盖已有内容。
- 【调试日志注入】在 generate.py 的 generate_paragraph 路由中注入了 📝/🎯 级别调试日志，可追踪每次请求的 mode、范文匹配结果、Prompt 路由决策。

#### ⏳ To-Do
- 验证替换模式是否正确走 REPLACE_PROMPT 路径。

---

### 📅 2026-04-11 15:21 (大纲提取重复问题修复)
#### ✅ Done
- 【大纲提取重复根治】定位到上传 Word 文档时大纲提取产生 164/467 重复条目的三个根因并全部修复：(1) template.py 新增 TOC 目录样式过滤（排除 Word 自动目录区段落）；(2) 标题清洗——去除制表符+页码残留（如 `7.1.5 土方流向	569` → `7.1.5 土方流向`）；(3) 基于标题文本的去重机制（保留首次出现）。
- 【编号列表误判修复】heading_utils.py L1 正则从 `^\d+[\s\.]` 改为 `^\d+\s+\S` / `^\d+\.\s+\S`，要求数字后必须跟空格，排除了 `1.自然因素`、`2.人为因素` 等编号列表项被误判为 L1 标题的问题。

#### ⏳ To-Do
- 用户需重新上传该 Word 文档验证大纲提取效果。

---

### 📅 2026-04-11 23:05 (Mermaid 图表引擎集成)
#### ✅ Done
- 【Mermaid 全栈集成】完成流程图/技术路线图自动生成能力：前端 MermaidBlock.tsx 可视化编辑器（支持预览/代码/可视化三种模式，可添加/删除/编辑节点、切换布局方向）；后端 Prompt 条件注入 Mermaid 语法参考（匹配标题关键词）；Word 导出时通过 mermaid.ink API 将代码块渲染为 PNG 嵌入。
- 【ARM64 兼容适配】发现 OrbStack ARM64 容器无法运行 Puppeteer/Chromium，将 mmdc 本地渲染方案改为 mermaid.ink 在线 API（免费、无本地依赖、验证通过）。
- 【num_predict 提升】LLM 单次生成 Token 上限从 8192 提升至 12288，解决工程报告长章节被截断问题。

#### ⏳ To-Do
- 用户端到端测试：在含"技术路线"、"工作流程"等标题的章节生成内容，验证 Mermaid 图表自动生成和渲染效果。

---

### 📅 2026-04-11 23:25 (表格渲染引擎与智能 Persona)
#### ✅ Done
- 【表格渲染 Bug 修复】修复了刚上线的 Markdown 表格转 HTML 引擎失效问题。根因是 Tiptap 的 `editor.getHTML()` 会将纯文本自动包裹在 `<p>` 标签中，导致原有的严格行首起点的正则表达式 (`^\|`) 无法匹配被 `<p>|` 污染的第一行。优化了正则容忍度并增加了 `<p>` 标签清洗脱壳逻辑。
- 【AI Persona 智能推荐】在 AgentChat 配置侧边栏新增「🪄 智能推荐」功能，点击后后端通过 `/api/recommend-persona` 端点扫描项目已上传文件名及 RAG 内容特征，动态组装 Meta-prompt 并调用 LLM 一键生成符合当前项目行业及约束的超高质量角色提示词。

#### ⏳ To-Do
- 需要用户在真实数据项目中测试智能生成的 Persona 质量。
- 需要用户测试表格自动转换和样式渲染效果。

---
