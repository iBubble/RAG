📅 2026-04-08 15:38 (修复核心推理引擎 import 幻觉 & LLM 检查恢复)

✅ Done:
- 【故障排查与修复】快速定位 `core/llm_engine.py` 中因重构遗漏的 `settings` 导入错误，修复了 `check_ollama_availability` 崩溃。
- 【部署验证】热补丁应用成功，PM2 重启后 `/api/llm/status` 接口恢复 200 OK，Ollama 通信正常。
- 【日志监控】经实时日志追踪，确认后端 API 已无 500 异常，全链路状态检查点恢复通畅。

---

📅 2026-04-08 15:55 (修复 Chat 角色偏差与 44s 推理延迟)

✅ Done:
- 【角色优先级重构】重构 `chat` 接口的 `sys_prompt` 组装逻辑，确保项目定义的 `custom_persona` 具有最高优先级。
- 【注入禁思指令】针对简单询问强制注入 `NO_THOUGHT` 提示词，将响应延迟从 40s+ 降低至秒级。
- 【风格红线加固】在快车道 Prompt 中注入"态度冰冷、禁止寒暄"强约束，防止多余人工痕迹。

---

📅 2026-04-08 16:50 (重构 Chat 多模式架构 & SSE 思考链物理过滤)

✅ Done:
- 【多模式语义分流】重构 `generate.py` 的 `chat` API，确立四大对话模式（Fast/Deep/Expert/General），实现针对性 Prompt 注入与参数调优。
- 【RAI 物理过滤】实现在线 SSE 思考链过滤逻辑（`think_mode="filter"`），动态截断 `<think>` 标签内容。
- 【人设专家化】注入"四川胜垚工程咨询资深总工"专家人设，实现 Project/Persona 动态分流逻辑。

---

📅 2026-04-08 23:35 (跨模式 E2E 测试完成 & 性能瓶颈定位)

✅ Done:
- 【E2E 测试覆盖】创建并执行 6 个跨模式测试用例（fast/deep/expert/general + 简单问题快车道），SSE 流稳定性、Think 过滤、RAG 检索、角色注入全部正确。
- 【冷启动瓶颈定位】发现首次推理 TTFT 高达 85s（模型冷启动），后续请求稳定在 0.4-4s，确认 `keep_alive=-1` 在跨 Docker 网络场景下存在失效风险。
- 【测试基础设施】建立 `tests/test_chat_e2e.py` 自动化测试脚本，支持一键回归验证。

---

📅 2026-04-08 23:43 (模型预热 + 心跳守护机制上线)

✅ Done:
- 【模型预热】在 `llm_engine.py` 新增 `warmup_model()` 函数，FastAPI startup 事件自动触发空 prompt + `keep_alive=-1` 预热，强制模型常驻 GPU。
- 【心跳守护】新增 `start_model_heartbeat()` 后台 asyncio 任务，每 240s 向 Ollama 发送 keep_alive 请求，防止跨 Docker 网络超时卸载。
- 【日志可见化】为 `llm_engine` 配置 StreamHandler，预热/心跳日志由 PM2 stderr 管道可监控。

---

📅 2026-04-08 23:49 (is_simple 判别优化 + /no_think 注入)

✅ Done:
- 【触发词扩充】`_is_simple_query` 从 12 个触发词扩展到 28 个（含精确匹配+包含匹配两层），覆盖"嗨/在吗/你好呀/谢谢/你能做什么"等场景。
- 【推理截断】简单问题注入 `num_predict=256` + `num_ctx=2048`，从物理层面限制模型内部 think 推演空间。
- 【/no_think 注入】在简单问题 Prompt 中追加 qwen3 系列 `/no_think` 模型级指令，6/9 用例稳定 < 1s。

⏳ To-Do:
- [ ] 同步前端代码，确保 `chat_mode` 高级参数在 UI 端实时透传。
