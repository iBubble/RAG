# 工作状态存档 (ShengyaoRAG - Micro-Checkpoint)

📅 2026-04-19T08:45:00+00:00
✅ Done:
- 修复 Mac Studio 段落生成崩溃：在 `main.py` 注入 onnxruntime 阻断器，阻止 ARM/QEMU 模拟环境下 onnxruntime C++ 底层死锁/段错误导致后端反复重启。
- 在 `ecosystem.config.js` 添加 `OMP_NUM_THREADS=1` 等线程限制环境变量 + `PYTHONFAULTHANDLER=1` 崩溃堆栈追踪。
- 端到端验证通过：RAG 检索（BGE-M3 Dense+Sparse）+ LLM 流式生成均正常，后端 0 重启运行稳定。
⏳ To-Do:
- 用户在 Mac Studio 上实测「一键生成全文」，确认前端批量生成不崩溃。
- 另一台电脑登录崩溃问题：建议用户关闭浏览器翻译插件后重试。
- JWT_SECRET 建议加长至 32 字节以上，消除 InsecureKeyLengthWarning。
