📅 2026-05-08T00:21:43

 Done
- 修复  旧数据查询 Bug：允许  的实体匹配，恢复聊天中的混合检索和图谱上下文
#- 重置学习进度：清理 `.job_states` 文件，并重新排列入队共 560+ 份
import  531 份、RAG 测试 20 份、地下水项目 12 份）。
- 验证 Celery 任务管线：确认真实图谱三元组提取与 Neo4j 入库正在 `slow_queue` 队列中静默运转。

   To-Do
- 等待 Celery 队列静默消费完成（数百份文件的切片和 LLM 图谱抽取）。
- 等待入库完成后，为各个项目上传并保存《范文》，触发第三阶段的智能预计算任务


📅 2026-05-08T00:20:00

 Done
- 修复 `graph_rag.py` 旧数据查询 Bug：允许 `project_id IS NULL` 的实体匹配，恢复聊天中的混合检索和图谱上下文注入。
- **代码回归修复**：由于 Git pull --rebase 导致的后端代码回退已手动修复。重新实现了  接口，解决了前端 404/401 报错。 `.job_states` 文件，重新入队共 560+ 份文件的解析任务（含 531 份、RAG 测试 20 份、地下水项目 12 份）。
- **存储路径修正**： 已修正，核心元数据  现在强制指向 RAID 卷 ()，确保后续数据的持久性。 Neo4j 入库正在 `slow_queue` 队列中静默满载运

   To-Do
## ⏳ To-Do 
- 等待入库完成后，为各个项目上传并保存《范文》，触发第三阶段的智能预计算任务。


📅 2026-05-08T01:12:46

 Done
echo === Collections ===  `current_task` 追踪。
- 底层文件嗅探：扫描 `.job_states` 读取文件状态及磁盘大小（MB/KB），利用 Redis `precompute:current_section` 热埋点实时同步 GPU 当前学习篇章。
# Get collection name from code Loader2 旋转动效图标、动态副标题，并做弹性文本截断，防爆版长文件名。

   To-Do
- 确保系统环境保持运行状态，让数百份文件持续消费完毕。
#- 等待入库彻底完工，继续推进确保系统环境保持运行状态，让数百份文件持续消费完毕。



 Done:
- 审计了从文件上传到对话检索的全链路逻辑。
curl -s --max-time 5 "$QDRANT/collections" | python3 -c "import sys,json; d=json.load(sys.stdin); [print(c['name']) for c in d['result'][' 64 候选），防止大量文件时截断关键 chunk。
# 2. 查这个项目有多少 chunks（用正确的 名） collection_name=_collection_name, 
   To-Do:
- 等待 Celery 慢速队列消化完毕后，可考虑开启大模型 Reranker 精排增强相关度排序。
- 未来可以考虑基于 LLM 引入 Query Rewriting（用户查询自动术语改写）。


---

📅 本次时间戳: 2026-05-08 10:55:00
✅ Done:
- 审计了从文件上传到对话检索的全链路逻辑。
- 优化了 vector_store 动态候选池大小（最高 64 候选），防止大量文件时截断关键 chunk。
- 为选定大量文件（>20）的对话场景启用了多级索引预筛选，减少跨文件污染。

⏳ To-Do (Pending Optimizations):
- **【中等投入】重新启用 Reranker (交叉编码器精排)**：等 Celery 慢速队列消化完毕且确认 ARM 算力允许后，可考虑开启轻量级 Reranker（或仅对 Top-8 候选进行二次打分），提升“近义但不同词”的语义排序。
- **【中等投入】查询改写 (Query Rewriting)**：在检索前用 LLM 将用户的口语化提问改写为结构化的工程术语/关键词集合，弥补口语和专业文档的语义鸿沟。
- **【深层提升】图谱数据清理**：彻底清理兼容旧版本的无归属 (`project_id IS NULL`) 图谱实体，消除跨项目泄漏的理论风险。
- **【深层提升】文档级元数据增强**：在向量切片阶段，注入通过大模型提取的更丰富的 payload 标签（如：文件类型、所属工艺模块等），让检索能够实现结构化过滤。


---

📅 本次时间戳: 2026-05-08 11:35:00
 Done:
- 在管理员学习进度看板中为每个项目增加了底层切片数和实体数的高级监控徽章。
- 更新了 vector_store 和 admin API 提供统计支持。
   To-Do:
- 继续监控 Celery 慢速队列消化情况。


---

📅 本次时间戳: 2026-05-08 11:49:00
 Done:
- 修复了项目列表切片数量不一致的 UI 数据残留问题（清理了 RAG测试项目 之前的脏数据 505 个 chunk）。
- 将因超大图纸 OOM 或崩溃卡死在处理队列中的 2-2山坪塘_99.pdf 强制置为了 failed 状态，使毗卢测试的看板恢复正常滚动推进。
   To-Do:
- 考虑 processing 的僵尸任务提供自动超时检测。


---

📅 本次时间戳: 2026-05-08 12:05:00
 Done:
- 执行了系统级深层数据清理 (Deep Cleanup)，从 Qdrant 中物理删除了 2879 个属于废弃项目的孤儿切片。
- 从 Neo4j 图谱数据库中删除了所有 执行了系统级深层数据清理
- 确保学习进度看板的数据展示 100% 精确反映当前活跃项目的真实知识密度。
   To-Do:
- 继续监控 Celery 队列处理情况。
📅 2026-05-08T10:16:10

 Done
- 修复图谱提取 asyncio 事件循环 Bug：graph_rag.py 的 extract 改为 async def，worker.py 用 loop.run_until_complete() 驱动，彻底消除 cannot run event loop 错误。
- 修复管理员看板 system-stats API：Neo4j 统计 _ensure_connection() 惰性初始化，Qdrant 已确认有 5182 切片。
- 已验证 Neo4j 成功写入 30 个三元组（首批），563 个文件正在后台队列中持续处理。

   To-Do
- 等待 563 个文件的图谱提取全部完成（预计数小时）。
- 刷新管理员看板确认 Qdrant 切片数和 Neo4j 实体/关系数实时增长。
- 图谱完成后推进范文上传和预计算阶段


