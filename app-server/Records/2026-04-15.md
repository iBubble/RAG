# 工作状态存档 (ShengyaoRAG - Micro-Checkpoint)

📅 2026-04-15T15:33:00+00:00
✅ Done:
- 完成第一梯队 RAG 架构升级：query_by_file_ids 从 3 级管线升级为 5 级管线（多级索引预筛选 → RRF融合 → Reranker精排 → 上下文膨胀 → 合并去重）。
- 新增 `backend/core/reranker.py`（bge-reranker-v2-m3 交叉编码器懒加载），修改 `backend/core/vector_store.py` 增加 `_hierarchical_prefilter` / `_expand_context` / doc_summary 入库逻辑。
- 新增迁移脚本 `backend/scripts/build_doc_summaries.py`，成功为 63 个文件生成摘要索引（Qdrant 9870→9945 points），后端重启验证通过 HTTP 200。
⏳ To-Do:
- 用户实际使用后观察 Reranker 首次加载延迟和检索质量提升效果。
- 后续可推进第二梯队：Adaptive RAG / GraphRAG / Document Augmentation。
