📅 2026-05-29 09:32

✅ Done
- 诊断出范文智能替换（replace/clone）模式下表格数据无法注入的根因：`generate_paragraph()` 和预计算引擎 `_generate_single_section()` 中完全没有调用 `query_tables()`，导致 LLM 只能看到被 semantic_chunk 切碎的行数据（无表头上下文）
- 在 `api/generate.py` 和 `core/precompute.py` 中同步新增表格注册表查询和完整 Markdown 表格注入逻辑，与聊天模式的 `query_tables()` 对齐
- 将 replace 模式 `_MAX_REPLACE_FACTS` 从 6000 提升到 8000，为完整表格预留空间

⏳ To-Do
- 用户前端验证：触发"耕地"章节 replace 模式生成，确认表 2-1 数据正确填充（毛楼村/荞子坡村/沙子坪村/雄峰村）
- 确认正文中耕地总面积替换为新项目数据（18147.7亩而非范文的 20255.09亩）

---

📅 2026-05-28 19:20

✅ Done
- 诊断并定位了《第二章_图像任务表主项目.pdf》（151页大文件）在进行 PDF 表格提取 `page.find_tables()` 时，因某些页面存在超高密度的 CAD 矢量路径而导致几何计算陷入死循环挂起（CPU 346%）的严重隐患
- 在 `core/extractors/pdf.py` 中注入防卡死机制：若单页 drawings 数超过 500 条，则自动跳过该页的表格检测，杜绝死循环
- 重启 PM2 并使用脚本重新激活任务，目前 Celery Worker 运行平稳，图谱队列正常流转

⏳ To-Do
- 观察《第二章_图像任务表主项目.pdf> 出队并完成向量化，确认防卡死机制完全生效
- 检查慢队列全部消化完毕后的检索命中结果

---

📅 2026-05-28 16:51

✅ Done
- 诊断并排查了 PM2 重启后部分文件卡在 `processing`、`graph_extracting`、`graph_queued` 的僵死任务状态
- 编写并运行了 `resume_stuck_tasks.py`，成功将 12 个由于服务重启中断的任务状态重置并重新投递至 Celery 对应队列中
- 确认快慢队列目前正在分别处理 `川府函_2021_8号_四川省用水定额.pdf` 和 `泸县2025年高标准农田新建项目_毗卢片区_.xlsx`，入库流程已全部恢复正常运转

⏳ To-Do
- 监控该批次重入库的 12 个文件的后续状态变更，直至全部转为 `vectorized`
- 验证 Excel 和 PDF 表格提取后检索命中功能
