# 技术决策记录 (ADR)

## ADR-001: 视频与音频多模态文件解析技术选型

### 背景
系统需要支持对视频（mp4/mov）和音频（mp3/wav）文件进行语义检索。这就需要先将音频或视频中的语音内容提取为纯文本，再将其送入向量化管道。

### 备选方案对比

#### 方案 1: 本地 ffmpeg + transformers ASR Pipeline (Whisper-Base)
- **原理**：调用系统级 `ffmpeg` 命令进行格式标准化（转为 16kHz 单声道 wav），再通过 PyTorch/transformers 的 ASR Pipeline 运行 `openai/whisper-base` 本地模型识别语音。
- **优点**：100% 本地化，安全合规，对 CPU/GPU 性能开销适中，识别率高，扩展性强。
- **缺点**：容器内需要安装 `ffmpeg`，以及首次运行需在线下载 whisper 权重（约 74MB）。

#### 方案 2: Ollama 视觉模型分帧结合分析
- **原理**：将视频分帧送至 `qwen2.5vl:7b` 视觉模型进行关键帧汇总描述。
- **优点**：可以捕捉图像细节。
- **缺点**：显存开销极大，容易造成 GPU 击穿，且完全丢失了当事人录音的语音对话内容。

#### 方案 3: 云端第三方 Whisper API 接入
- **原理**：直接将音视频文件上传到外部云端接口进行转写。
- **优点**：本地开发无需配置任何 ASR 模型和 ffmpeg。
- **缺点**：不符合本地安全及企业图纸/证据保密隔离原则，需要外部 Key。

### 技术决策
**选择【方案 1】**。由于该系统是私有化、局域网安全部署的 RAG 知识库，且运行在 MacBook Pro / Mac Studio 等高性能设备上，本地 Whisper-Base 能在保障隐私的前提下提供毫秒级的极速语音识别响应。

## ADR-002: macOS 休眠唤醒后外部存储挂载自愈机制

### 背景
MacBook Pro (M4 Max) 在关盖休眠或唤醒后，Docker Desktop 的 VirtioFS / gRPC-FUSE 会导致外置挂载卷（`/Volumes/macData/...`）发生挂载句柄失效（Stale File Handle），容器内访问目录呈现悬空 inode，触发系统脱机报警与只读降级。

### 备选方案对比
1. **方案 1：宿主机统一守护巡检与自愈（crontab/脚本秒级自愈）**
   - 性能极高、无侵入性、可维护性强，能够无缝覆盖睡眠唤醒、热插拔与偶发挂载中断。
2. **方案 2：macOS 原生休眠唤醒事件监听（sleepwatcher/IOKit）**
   - 依赖外部系统级扩展，无法处理非睡眠场景下的挂载断裂。
3. **方案 3：容器内挂载 Docker Socket 联动自救**
   - 侵入业务代码，暴露 Docker.sock 存在安全隐患。

### 技术决策
**选择【方案 1】**。采用宿主机层面的定时巡检守护机制（`storage_watchdog.sh`），在宿主机磁盘正常但容器内挂载失效时秒级执行自愈热重启，同时纳管 `GenRAG-Server` 与 `RAG-Server`。

## ADR-003: 知识库未就绪前置阻断与算力熔断守卫机制

### 背景
文档上传后需要经历多模态解析、切片（Chunking）与向量化入库等异步步骤。在该学习窗口期内，知识库中尚无有效切片。若允许用户在此期间无阻断对话，系统会将空上下文输入给 27B 大模型，造成严重的本地 GPU 算力浪费（长时间显存占用与 Prefill 阻塞），且输出“核心证据缺失/内容为乱码”等无效甚至误导性的答复。

### 备选方案对比
1. **方案 1：纯前端 UI 状态门禁**
   - 依赖客户端轮询与输入框置灰。无法防范绕过前端直接调用 API 的场景，且无法感知深层检索召回质量。
2. **方案 2：后端就绪度守卫与检索零召回熔断（推荐）**
   - 在 API 层执行前置轻量拦截（耗时 <5ms），若目标文件未完成切片或检索结果零召回，直接短路返回等待提示，100% 避免大模型 GPU 空转。
3. **方案 3：端到端分级长连接状态总线**
   - 引入 WebSocket 状态机，维护全链路三级就绪度，但改造链条较长。

### 技术决策
**选择【方案 2 为主，方案 1 为辅】的双层漏斗机制**。
- 后端在 `/api/chat` 入口处实现 `check_readiness_for_chat`，针对指定文件处于处理中或案卷全量处理中实施刚性拦截，并在检索阶段增加零召回短路机制；
- 前端在聊天输入框上方提供动态轻量状态感知条，实现操作预期透明化与算力绝对保护。

