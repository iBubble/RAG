# 2026-09-01 23:07 升级“调查取证”与新增“研判裁量”全流程执法文书闭环

## 业务需求变更
1. 将原“案情研判”Tab 改名为“调查取证”，专用于推荐案源初查、现场检查、调查询问、抽样取证及强制措施等取证过程文书；
2. 新增“研判裁量”Tab，依据《行政处罚法》及裁量基准，推演处理结果（一般处罚/从轻处罚/减轻处罚/责令改正免罚等），并在 56 种文书中推荐终结报告、法制审核、告知听证、责令改正、处罚决定及结案审批等处理结果文书。

## 核心实现
1. **后端引擎更新 (`api/triage_judgment.py`)**：
   - 增加持久化目录 `settings.DATA_DIR / "adjudication"`；
   - 增加 `GET/POST /api/projects/{project_id}/investigation/recommend` 路由（与原有 `judgment` 平滑兼容）；
   - 编写 `run_adjudication_inference` 与 `sanitize_adjudication_result`，支持裁量定性、大模型法律推演、互斥校验（处罚决定书 vs 不予处罚决定书二选一）；
   - 暴露 `GET/POST /api/projects/{project_id}/adjudication/recommend` 接口与状态持久化。
2. **前端界面开发**：
   - **`InvestigationPanel.tsx`**：将原有案情研判面板改造为调查取证主面板，图标使用 `ClipboardCheck`；
   - **`AdjudicationPanel.tsx`**：全新研判裁量主面板，配置专用紫色调主题徽章、大模型处理结果裁量意见条、图二 A4 微缩纸质卡片网格；
   - **`App.tsx`**：导航栏升级为 `智能助手` | `分拣填报` | `调查取证` | `研判裁量` | `AI表格` | `定制文档`。
3. **实机推理与验证**：
   - 本地案卷实机推演：定性为“一般处罚”，精准推荐《35.案件调查终结报告》、《36.案件审核表》、《38.行政处罚听证告知书》、《33.责令改正通知书》、《45.行政处罚决定书》、《53.结案审批表》；
   - 编译前端 `index-v4-D_vq_DRy.js` 并完成 `pm2 reload`，服务运行正常。
