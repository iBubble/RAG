# 工作变更记录 (2026-06-27 02:35)

## 变更文件列表
- `backend/scratch/test_d4_d5.py`
- `backend/core/vector_store.py`
- `backend/core/graph_rag.py`
- `backend/worker.py`

## 变更详情说明
### 1. 测试端口更正
- 将测试脚本中的网关交互端口由 `8001` 修改为 `8003`，适配项目 `ecosystem.config.js` 实际配置的网关端口。

### 2. 双层图谱增量写入实现
- 在 `vector_store.py` 中，实现 `get_all_chunks_with_payload` 以提取 Qdrant 中保存的完整 payload 信息（如 `semantic_role`, `page_number` 等）。
- 在 `graph_rag.py` 中，定义了 `ingest_doco_and_form_relations` 方法。对于每一个 EvidenceUnit，根据排版特征特征自动将其标记为 DoCO 元素，并建立 `Document -[HAS_ELEMENT]-> EvidenceUnit` 关系（第一层）；对分块文本匹配市监表单字段的规则特征，建立 `FormField -[EVIDENCE_BY]-> EvidenceUnit` 关系（第二层）。
- 修改 `worker.py`，将分块抽取与 DoCO / FormField 图谱提取链路整合。

### 3. 增量清理级联逻辑
- 修改 `graph_rag.py` 中的 `delete_by_file_id` 方法，实现对特定 `file_id` 的 EvidenceUnit、Document 节点和孤立的 FormField 节点的彻底 `DETACH DELETE` 清理，确保多次上传不产生冗余重复关系。
