# 修改内容纪实

## 📅 记录时间：2026-06-06 12:31
### 🛠️ 本次修改内容总结
1. **看板卡片 Header 布局调整**：
   - 彻底重构了 [LearningProgress.tsx](file:///Users/gemini/Projects/Own/RAG/app-server/frontend/src/components/Admin/LearningProgress.tsx) 看板项目的 Header 逻辑。将优先级下拉选择框、手动暂停按钮和状态显示 Label 统一整合并挪到了 Header 的最右侧，左侧仅保留案件名称，布局更加紧凑。
2. **手动暂停学习与恢复逻辑**：
   - 数据库表 projects 执行增量迁移，扩展并新增了 `is_paused` 列（默认 0），并完成了后端 API 的 PUT 参数及 `/learning-progress` 接口的适配。
   - 前端新增了“暂停/恢复”按钮并绑定了 `handleTogglePause` 行为。
   - 在 [worker.py](file:///Users/gemini/Projects/Own/RAG/app-server/backend/worker.py) 的 `should_pause_project_task` 退避决策中引入了 `is_paused` 检查。若该项目被手动暂停，则其下属所有 Celery 任务在进入时会自动退避重新抛回队列等待，让出 Worker 槽位，且暂停的项目不影响其他正常项目的学习进度。
3. **学习状态徽章 Label 展示**：
   - 新增了状态展示 Label，能够根据各案件下属文件的解析进度及 `is_paused` 的状态，精准实时展示 **“暂停中”**、**“学习中”（带呼吸灯闪烁）**、**“排队中”** 及 **“已完成”**。
4. **编译与服务重启**：
   - 修复了 `LearningProgress.tsx` 中 `isVectorizing` 和 `isGraphing` 因声明未使用导致的 TypeScript `noUnusedLocals` 构建报错。
   - 成功完成容器内前端编译并重启了 PM2 托管的所有进程，数据库增量自动迁移就绪，系统运行平稳。
