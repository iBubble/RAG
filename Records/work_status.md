# 工作状态记录

## 📅 2026-06-30 07:35
*   **解密 Ollama 并发死锁与关系图谱恢复**：
    - **病因诊断**：宿主机 Ollama 显存中挂起的旧 llama-server 进程（已挂载 17 小时）发生底层 Metal 死锁，导致新的推理请求无法入队，并向 Celery 慢队列疯狂抛出 `503 server busy` 状态，图谱提炼进程因此卡死在 `0 / 1160`。
    - **强杀与拉起**：强杀了宿主机上卡死的 `llama-server` 和 `ollama` 进程，通过 `open -a Ollama` 重新拉起。
    - **接力畅通验证**：Ollama 重新启动后，Celery 慢队列已成功连接宿主机 11434 端口。后续接力批次（`compute_community_summaries`）请求全部返回 `200 OK` 并快速生成写入摘要，图谱提炼进度已完全恢复正常运行。

## 📅 2026-06-30 07:37
*   **宿主机 Ollama 自愈 Watchdog 守护任务上线**：
    - **脚本持久化**：在项目目录 `/Users/gemini/Projects/Own/RAG/scripts/ollama_watchdog.sh` 下持久化了自愈探测脚本，配置 8 秒超时阈值与 3 次重试熔断机制。
    - **计划任务注册**：为脚本赋予了 `chmod +x` 执行权限，并使用安全的并发合并命令将其成功注册在 macOS 宿主机的 `crontab` 任务中，设定为每分钟自动探测一次，并在检测到死锁或连续 503 时强制复苏 Ollama，消除了以后的锁死隐患。

## 📅 2026-06-30 07:49
*   **自愈重试定时任务与滑动开关交互优化**：
    - **定时故障重试（自愈）**：在 Celery Beat 定时引擎中添加并配置 `'auto-recover-learning-faults'` 定时调度，执行频率为每 5 分钟（`300`秒）自动运行一次。定时任务负责自动识别因底层故障失败的文件，并在 3 次上限阈值内自动重新推入 Celery slow/fast 队列。
    - **滑动开关（Switch）交互升级**：将文档树顶部的“引用所有公共文档”复选框改造为带有平滑滑动微动画的现代 iOS 风格 Switch 开关，文字与开关两端对齐排版。初始值保持 `true`（默认打开）。

⏳ To-Do:
- 无