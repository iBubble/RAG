# 工作状态记录

## 📅 2026-06-28 03:22
✅ Done:
- 彻底解决 Smart (深度思考) 模式下，因受控 `<details>` 元素被流式数据高频刷新重绘导致的 Chrome 浏览器标签页崩溃（错误代码 5）问题。
- 重构并清理了 `<think>` 标签的切分逻辑，消除了就地 Mutation 修改数组引起的 DOM 树 diff 冲突隐患。
- 采用静态 div 模拟正在流式推理中的思考组件，并在结束流式输出后平滑渲染为原生折叠 details 容器。

## 📅 2026-06-28 04:00
✅ Done:
- **Zustand 流式事件批处理 (Batching) 优化**：针对前端高频重绘做 Batching 合并，渲染频率和系统负荷直接降低百倍，避免 OOM。
- **前端流式 Markdown 纯文本降级防护**：在流式输出中采取纯文本降级渲染（保留换行），结束后瞬间平滑切回 Markdown 渲染。

## 📅 2026-06-28 04:15
✅ Done:
- **去司法/非行政化用词与图标重构（政府机关公文适配）**：
  * **提示词端强约束**：重构了 Go 网关 `eino_graph.go` 中定性审计（Auditor）节点的 System Prompt，屏蔽司法裁决与法庭色彩。
  * **三端命名清洗**：在 Python 端加了拦截过滤；Go 端增加了 `cleanGovernmentRoleName` 过滤器；前端将 `arbiter` 渲染图标由 `👑` 修正为 `📝`。

## 📅 2026-06-28 04:20
✅ Done:
- **深度思考进度提示前缀优化**：在 `projectStore.ts` 中，若 `chatMode === 'smart'`（深度思考模式），在拼装流式 Progress 文本之前，自动补全并冠以 `【深度思考】模式开启：` 前缀。

## 📅 2026-06-28 04:45
✅ Done:
- **大模型核心内容召回修复**：恢复了 Go 网关 `eino_graph.go` 中终审节点简练有效的原始 System Prompt 范式，只更替其中的角色命名。消除了原先由于指令过于臃肿负向堆砌导致本地大模型（Qwen35B MOE）生成内容严重缩水的故障。
- **文献区块断行与字体不一彻底治理**：在前端 Markdown 预处理逻辑中增加刚性正则清洗，合并 `【参考文档区块` 与其数字之间不合时宜的换行符和多余空格。从而使引用内容与上一行段落完美连属，根治了由此导致段落裂变为两个 `<p>` 标签引发的首行缩进及字体不一格式 bug。